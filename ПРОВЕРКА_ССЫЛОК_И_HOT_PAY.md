# ✅ Проверка ссылок и HOT Pay — результаты

**Дата проверки:** 2026-02-17  
**Проблема:** HOT Pay checkout не открывается после клика "Оформить и продолжить"

---

## 🔍 Что было проверено

### 1. Backend API URL ✅
- **Настроен:** `https://telegram-miniapp-ar09.onrender.com`
- **Прописан в:** `bot/public/index.html` и `public/index.html`
- **Проверка:** Все API эндпоинты доступны

### 2. HOT Pay эндпоинты ✅
Все необходимые эндпоинты присутствуют в `bot/index.js`:
- ✅ `POST /api/payments/hot/create` — создание checkout URL
- ✅ `GET /api/payments/hot/status` — проверка статуса оплаты
- ✅ `POST /api/payments/hot/confirm` — подтверждение оплаты
- ✅ `POST /api/payments/hot/webhook` — webhook от HOT

### 3. HOT Pay URL формирование ✅
**Функция:** `buildHotCheckoutUrl()` в `bot/index.js` (строка 302)

**Параметры checkout URL:**
- `item_id` — ID платёжной ссылки из HOT админки
- `order_id` — уникальный ID заказа
- `memo` — дублирует order_id (требование HOT для webhook)
- `amount` — сумма платежа
- `currency` — валюта (USDT по умолчанию)
- `request_id` — ID заявки в нашей системе
- `sku` — тип продукта (single_song, couple_song, transit_song)
- `redirect_url` — URL для возврата после оплаты

**Базовый URL:** `https://pay.hot-labs.org/payment` (из переменной `HOT_PAYMENT_URL`)

### 4. Redirect URL ✅
**Настроен:** `https://telegram-miniapp-ar09.onrender.com?payment=success&request_id=...`

Это правильный формат для возврата в Mini App после оплаты.

---

## 🛠️ Исправления, которые были применены

### 1. Многоуровневый fallback для открытия HOT Pay ссылки

**Попытка 1:** `Telegram.WebApp.openLink(checkoutUrl)`
- Рекомендуемый способ для Telegram Mini App
- Открывает ссылку во встроенном браузере Telegram

**Попытка 2:** `window.open(checkoutUrl, '_blank', 'noopener,noreferrer')`
- Fallback для обычного браузера
- Проверяет, что окно действительно открылось (не заблокировано popup-блокировщиком)

**Попытка 3:** Копирование ссылки в буфер обмена
- Если все попытки провалились
- Показывает ссылку пользователю для ручного открытия

### 2. Детальное логирование

Добавлены `console.log` на каждом этапе:
1. `[HOT] Требуется оплата, requestId: ...`
2. `[HOT] Отправляю запрос на /api/payments/hot/create`
3. `[HOT] Ответ получен, status: 200, ok: true`
4. `[HOT] Распарсенный ответ: { success: true, hasCheckoutUrl: true }`
5. `[HOT] Получен checkout_url: https://pay.hot-labs.org/payment?...`
6. `[HOT] Использую Telegram.WebApp.openLink` или `[HOT] Использую window.open fallback`
7. `[HOT] openLink вызван успешно` или `[HOT] window.open успешно открыл новое окно`

### 3. Обработка ошибок

- Если `checkout_url` не получен → `throw new Error('Checkout URL не получен')`
- Если все попытки открытия провалились → показываем ссылку пользователю
- Если popup заблокирован → `window.open` вернёт `null`, переходим к попытке 3

---

## 🧪 Как проверить

### Шаг 1: Откройте Mini App из Telegram
Нажмите на кнопку меню бота (должна быть настроена на `https://telegram-miniapp-ar09.onrender.com`)

### Шаг 2: Заполните форму
- Имя (2+ символа)
- Дата рождения
- Место рождения (выберите из подсказок)
- Время рождения или отметьте "Не знаю"
- Пол
- Язык
- Запрос (15+ символов)

### Шаг 3: Нажмите "К оплате"
Должна открыться страница оплаты с:
- ✅ Зелёным индикатором "✓ Страница оплаты загружена"
- Тарифами и ценами
- Полем для промокода
- Кнопкой "Оформить и продолжить"

### Шаг 4: Нажмите "Оформить и продолжить"
1. Откройте DevTools (если возможно) → Console
2. Следите за логами с префиксом `[HOT]`
3. Должна открыться страница HOT Pay в новом окне/вкладке

### Шаг 5: Проверьте логи
Если HOT Pay не открылся, в консоли будет видно:
- На каком этапе произошла ошибка
- Какой метод открытия использовался
- Почему он провалился

---

## 📋 Возможные проблемы и решения

### Проблема 1: Popup заблокирован браузером
**Симптом:** `window.open` возвращает `null`  
**Решение:** Разрешите popup для `telegram-miniapp-ar09.onrender.com` в настройках браузера

### Проблема 2: `Telegram.WebApp.openLink` не работает
**Симптом:** Ссылка не открывается, в консоли `[HOT] Telegram.WebApp.openLink failed`  
**Решение:** Убедитесь, что приложение открыто из Telegram (не в обычном браузере)

### Проблема 3: `checkout_url` не получен от backend
**Симптом:** Ошибка "Checkout URL не получен"  
**Решение:** 
1. Проверьте переменные окружения в Render:
   - `HOT_PAYMENT_URL` (должен быть `https://pay.hot-labs.org/payment`)
   - `HOT_ITEM_ID_DEFAULT` или `HOT_ITEM_ID_SINGLE_SONG`
2. Проверьте логи backend в Render Dashboard

### Проблема 4: HOT Pay показывает ошибку "Invalid item_id"
**Симптом:** Страница HOT Pay открылась, но показывает ошибку  
**Решение:**
1. Зайдите в [pay.hot-labs.org](https://pay.hot-labs.org)
2. Проверьте, что платёжная ссылка с этим `item_id` существует и активна
3. Скопируйте правильный `item_id` и обновите переменную в Render

---

## 🚀 Деплой

**Статус:** ✅ Изменения запушены в main  
**Commit:** `49672d2` — "Исправление открытия HOT Pay: многоуровневый fallback + детальное логирование"  
**Render:** Автоматический деплой запущен

---

## 📞 Если проблема сохраняется

Пришлите:
1. **Скриншот консоли** (DevTools → Console) с логами `[HOT]`
2. **Скриншот страницы оплаты** (если она открылась)
3. **Описание:** что происходит после клика "Оформить и продолжить"

Это поможет точно определить, на каком этапе происходит сбой.
