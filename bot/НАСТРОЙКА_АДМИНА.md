# Настройка команды /admin в боте YupSoul

Команда **/admin** показывает список последних заявок из приложения и присылает ссылку на **веб-админку** (заявки, этапы генерации, перезапуск). Её видят только пользователи, чей Telegram ID указан в настройках бота. Админку можно также открыть из Mini App (кнопка «👑 Админка» на главной у админов) или в браузере по ссылке с главной страницы бота.

---

## 1. Внутренняя структура (как это устроено)

- **Команда в коде:** бот обрабатывает сообщение `/admin` и проверяет, есть ли отправитель в списке админов.
- **Список админов** задаётся в файле `bot/.env` переменной **ADMIN_TELEGRAM_IDS** (один или несколько ID через запятую).
- **Данные заявок:** пока берутся из памяти бота (последние 100). Если подключён Supabase — из таблицы `track_requests`.
- **В настройках Telegram (BotFather)** команды регистрировать не нужно — бот сам отправляет список команд при старте (`setMyCommands`). Пользователь видит их, когда в чате с ботом нажимает «/» или вводит команду.

---

## 2. Пошаговая настройка

### Шаг 1: Узнать свой Telegram ID

1. Открой Telegram и найди бота **@userinfobot**.
2. Напиши ему любое сообщение (например, «привет»).
3. Бот пришлёт твой **Id** — число вида `619065619`. Скопируй его.

### Шаг 2: Прописать ID в настройках бота

1. Открой в проекте файл **`bot/.env`** (в папке бота).
2. Найди строку **ADMIN_TELEGRAM_IDS**.
3. Впиши свой ID (или несколько через запятую без пробелов):

```env
ADMIN_TELEGRAM_IDS=619065619
```

Несколько админов:

```env
ADMIN_TELEGRAM_IDS=619065619,123456789
```

4. Сохрани файл.

### Шаг 3: Перезапустить бота

- Если бот запущен в терминале: нажми **Ctrl+C**, затем снова выполни:
  ```bash
  cd bot
  npm start
  ```
- В консоли при старте должно появиться: `Админы (ID): 619065619` (или твои ID). Это значит, что список админов подхватился.

### Шаг 4: Проверить в Telegram

1. Открой чат с ботом **от того аккаунта**, чей ID ты вписал в `ADMIN_TELEGRAM_IDS`.
2. Напиши команду **/admin** (или выбери её из списка после ввода «/»).
3. Бот должен ответить:
   - либо списком заявок («📋 Последние заявки…»),
   - либо сообщением «Заявок пока нет», если заявок ещё не было.

Если бот отвечает «У тебя нет доступа к этому разделу» — проверь, что в `.env` указан именно ID того аккаунта, с которого ты пишешь боту, и что бот был перезапущен после правки `.env`.

---

## 3. Что делать в настройках Telegram (BotFather)

- **Регистрировать команду /admin в BotFather не нужно.** Список команд бот задаёт сам при запуске.
- В BotFather настраивается только:
  - **Menu Button** — ссылка на Mini App (см. файл `ССЫЛКА_ДЛЯ_КНОПКИ_БОТА.txt` в корне проекта).

Команды `/start` и `/admin` появятся у пользователя в меню (кнопка «/» в поле ввода), как только бот успешно выполнит `setMyCommands` при старте.

---

## 4. Краткий чек-лист

- [ ] Узнал свой Telegram ID через @userinfobot.
- [ ] Вписал его в `bot/.env` в переменную `ADMIN_TELEGRAM_IDS`.
- [ ] Перезапустил бота (`npm start` в папке `bot`).
- [ ] В консоли при старте вижу строку «Админы (ID): …».
- [ ] В чате с ботом от этого аккаунта ввёл /admin и получил ответ (список заявок или «Заявок пока нет»).

После этого команда /admin считается настроенной.
