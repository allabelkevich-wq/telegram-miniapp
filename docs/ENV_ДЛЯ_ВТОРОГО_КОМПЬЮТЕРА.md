# Переменные окружения для второго компьютера

**ВАЖНО:** Файл `bot/.env` не попадает в Git (для безопасности), поэтому его нужно создать вручную на втором компьютере.

---

## 🔑 Обязательные переменные (минимум для работы)

Создай файл `bot/.env` на втором компьютере и добавь эти переменные:

```bash
# ══════════════════════════════════════════════════════════
# ОБЯЗАТЕЛЬНЫЕ ПЕРЕМЕННЫЕ (без них бот не запустится)
# ══════════════════════════════════════════════════════════

# Токен бота от @BotFather
BOT_TOKEN=твой_токен_от_BotFather

# ID админов через запятую (узнать свой: написать @userinfobot в Telegram)
ADMIN_TELEGRAM_IDS=твой_telegram_id

# URL Mini App (для кнопки меню в боте)
MINI_APP_URL=https://telegram-miniapp-ar09.onrender.com/app

# URL для Render (чтобы бот знал свой публичный адрес)
RENDER_EXTERNAL_URL=https://telegram-miniapp-ar09.onrender.com
```

---

## 🗄️ Supabase (для сохранения данных)

Если используешь базу данных:

```bash
# Supabase URL (Project Settings → API → Project URL)
SUPABASE_URL=https://твой_проект.supabase.co

# Supabase Service Key (Project Settings → API → service_role key)
SUPABASE_SERVICE_KEY=eyJ...твой_ключ
```

**Где взять:**
1. Открой https://supabase.com/dashboard
2. Выбери свой проект
3. Settings → API
4. Скопируй `Project URL` и `service_role` key

---

## 🤖 AI генерация (DeepSeek + Suno)

Если используешь автоматическую генерацию песен:

```bash
# DeepSeek API (для генерации текста и промптов)
DEEPSEEK_API_KEY=sk-твой_ключ_deepseek
DEEPSEEK_MODEL=deepseek-chat
DEEPSEEK_MAX_TOKENS=12000
DEEPSEEK_TEMPERATURE=1.5

# Suno API (для генерации музыки)
SUNO_API_KEY=твой_ключ_suno
SUNO_MODEL=V4_5ALL

# URL бэкенда (для callback от Suno)
BACKEND_URL=https://telegram-miniapp-ar09.onrender.com
```

**Где взять:**
- DeepSeek: https://platform.deepseek.com/api_keys
- Suno: https://sunoapi.org/api-key

---

## 💳 HOT Pay (для оплаты)

Если используешь оплату через HOT Pay:

```bash
# HOT Pay JWT токен
HOT_API_JWT=твой_jwt_токен

# HOT Pay webhook secret (для проверки подписи)
HOT_WEBHOOK_SECRET=твой_webhook_secret

# URL для оплаты
HOT_PAYMENT_URL=https://pay.hot-labs.org/payment
```

---

## 🔧 Дополнительные переменные (опционально)

```bash
# Webhook URL (для Render, чтобы избежать конфликта 409)
WEBHOOK_URL=https://telegram-miniapp-ar09.onrender.com

# Порт для локального запуска (по умолчанию 10000)
PORT=10000
HEROES_API_PORT=10000

# Админ секрет (для входа в админку через браузер)
ADMIN_SECRET=твой_секретный_токен

# Database URL (для миграций)
DATABASE_URL=postgresql://postgres:пароль@db.твой_проект.supabase.co:5432/postgres
```

---

## 📝 Пошаговая инструкция для второго компьютера

### 1. Склонируй репозиторий

```bash
git clone https://github.com/allabelkevich-wq/telegram-miniapp.git
cd telegram-miniapp
```

### 2. Создай файл bot/.env

```bash
# Создай файл
touch bot/.env

# Открой в редакторе
cursor bot/.env
# или
nano bot/.env
```

### 3. Скопируй переменные

**Вариант А: Скопировать с первого компьютера**

На первом компьютере:
```bash
# Покажи содержимое .env (БЕЗ отправки в чат!)
cat bot/.env
```

Скопируй вывод и вставь в `bot/.env` на втором компьютере.

**Вариант Б: Заполнить вручную**

Используй шаблон выше и подставь свои значения.

### 4. Проверь, что файл создан

```bash
# Проверь, что файл существует
ls -la bot/.env

# Проверь, что он НЕ попадёт в Git
git status
# Должно быть: "nothing to commit" (файл .env в .gitignore)
```

### 5. Установи зависимости

```bash
cd bot
npm install
cd ..
```

### 6. Готово!

Теперь можешь работать с проектом на втором компьютере.

---

## ⚠️ ВАЖНО: Безопасность

1. **НИКОГДА не коммить файл .env в Git!**
   - Он уже в `.gitignore`, но будь внимателен
   - Если случайно закоммитил — сразу смени все токены!

2. **Храни .env в безопасном месте:**
   - 1Password
   - Bitwarden
   - Зашифрованная флешка
   - Или просто скопируй с первого компьютера через защищённый канал

3. **Не отправляй .env в Telegram/WhatsApp/Email** — используй защищённые мессенджеры или менеджеры паролей

---

## 🆘 Если потерял .env

Если потерял файл `.env` и не помнишь значения:

1. **BOT_TOKEN:** Открой @BotFather → /mybots → выбери бота → API Token
2. **ADMIN_TELEGRAM_IDS:** Напиши @userinfobot в Telegram
3. **SUPABASE_URL и KEY:** Открой https://supabase.com/dashboard → Settings → API
4. **Остальные ключи:** Проверь в Render Dashboard → Environment Variables

---

## 📞 Нужна помощь?

Если что-то не работает:
1. Проверь, что все обязательные переменные заполнены
2. Проверь, что нет лишних пробелов в начале/конце значений
3. Проверь, что файл называется именно `.env` (с точкой в начале)
4. Запусти `npm run dev` в папке `bot/` и посмотри ошибки в консоли
