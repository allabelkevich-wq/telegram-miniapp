# Перенос проекта YupSoul на другой компьютер

## 📋 Что нужно установить на новом компьютере

### 1. Git
```bash
# macOS (если нет)
xcode-select --install

# Windows
# Скачать с https://git-scm.com/download/win

# Linux
sudo apt install git  # Ubuntu/Debian
sudo yum install git  # CentOS/RHEL
```

### 2. Node.js (версия 18 или выше)
```bash
# Проверить установлен ли Node.js
node --version

# Если нет — скачать с https://nodejs.org/
# Рекомендуется LTS версия (20.x или 22.x)
```

### 3. Текстовый редактор / IDE
- **Cursor** (рекомендуется, если работаешь с AI) — https://cursor.sh/
- VS Code — https://code.visualstudio.com/
- Или любой другой редактор

---

## 🚀 Шаги переноса

### Шаг 1: Клонировать репозиторий

```bash
# Перейди в папку, где хочешь разместить проект
cd ~/Projects  # или любая другая папка

# Клонируй репозиторий
git clone https://github.com/allabelkevich-wq/telegram-miniapp.git

# Перейди в папку проекта
cd telegram-miniapp
```

### Шаг 2: Настроить переменные окружения

#### Для бота (папка `bot/`)

```bash
# Скопируй пример файла .env
cp bot/.env.example bot/.env

# Открой bot/.env в редакторе и заполни:
nano bot/.env  # или любой редактор
```

**Обязательные переменные в `bot/.env`:**

```bash
# 1. Токен бота (получить у @BotFather в Telegram)
BOT_TOKEN=123456789:ABC-DEF...

# 2. ID админов (узнать свой: @userinfobot в Telegram)
ADMIN_TELEGRAM_IDS=123456789,987654321

# 3. URL Mini App (для кнопки меню в боте)
MINI_APP_URL=https://telegram-miniapp-ar09.onrender.com/app

# 4. URL для вебхуков (если деплой на Render)
WEBHOOK_URL=https://telegram-miniapp-ar09.onrender.com

# 5. Supabase (база данных)
SUPABASE_URL=https://fcnyhsmhvmliojonswcv.supabase.co
SUPABASE_SERVICE_KEY=eyJ...  # Project Settings → API → service_role

# 6. DeepSeek API (для генерации текста)
DEEPSEEK_API_KEY=sk-...  # https://platform.deepseek.com/api_keys

# 7. Suno API (для генерации музыки)
SUNO_API_KEY=...  # https://sunoapi.org/api-key

# 8. HOT Pay (для оплаты)
HOT_API_JWT=...
HOT_WEBHOOK_SECRET=...

# 9. Backend URL (для callback от Suno)
BACKEND_URL=https://telegram-miniapp-ar09.onrender.com

# 10. Database URL для миграций
DATABASE_URL=postgresql://postgres:ПАРОЛЬ@db.fcnyhsmhvmliojonswcv.supabase.co:5432/postgres
```

### Шаг 3: Установить зависимости

```bash
# Установить зависимости для бота
cd bot
npm install

# Вернуться в корень (если нужно установить зависимости для фронтенда)
cd ..
npm install  # если есть package.json в корне
```

### Шаг 4: Проверить работу локально

#### Запустить бота локально (для тестирования):

```bash
cd bot
npm start
```

Бот запустится в режиме polling (без вебхуков). Проверь в Telegram — бот должен отвечать на команды `/start`.

#### Просмотреть фронтенд локально:

```bash
# Из корня проекта
npm run preview
```

Откроется локальный сервер (обычно `http://localhost:3000`), где можно посмотреть `public/index.html`.

---

## 🔐 Важные файлы с секретами

**НЕ коммить в git:**
- `bot/.env` — содержит токены и ключи API
- Эти файлы уже в `.gitignore`, но убедись что не добавляешь их случайно

**Где взять секреты:**
1. **BOT_TOKEN** — у @BotFather в Telegram
2. **SUPABASE_URL и SUPABASE_SERVICE_KEY** — в панели Supabase (Project Settings → API)
3. **DEEPSEEK_API_KEY** — https://platform.deepseek.com/api_keys
4. **SUNO_API_KEY** — https://sunoapi.org/api-key
5. **HOT_API_JWT и HOT_WEBHOOK_SECRET** — в панели HOT Pay

---

## 📦 Структура проекта

```
telegram-miniapp/
├── public/               # Фронтенд (GitHub Pages)
│   ├── index.html       # Основной файл Mini App
│   └── assets/          # Картинки, стили
├── bot/                 # Бэкенд (Render)
│   ├── index.js         # Telegram бот + API
│   ├── heroesApi.js     # API для "Мои герои"
│   ├── deepseek.js      # Интеграция с DeepSeek
│   ├── package.json     # Зависимости
│   ├── .env.example     # Пример переменных окружения
│   └── public/          # Копия фронтенда для Render
├── docs/                # Документация
├── .gitignore           # Исключения для git
└── README.md            # Описание проекта
```

---

## 🌐 Деплой (если нужно задеплоить на свой сервер)

### GitHub Pages (фронтенд)
Уже настроен! Деплой происходит автоматически при push в `main`:
- Папка `public/` деплоится на `https://allabelkevich-wq.github.io/telegram-miniapp/`

### Render (бэкенд + бот)
1. Зайди на https://render.com/
2. Подключи GitHub репозиторий
3. Создай Web Service:
   - **Root Directory:** `bot`
   - **Build Command:** `npm install`
   - **Start Command:** `npm start`
4. Добавь переменные окружения (Environment Variables) из `bot/.env`
5. Деплой запустится автоматически

---

## ✅ Проверка после переноса

1. **Git работает:**
   ```bash
   git status
   git log --oneline -5
   ```

2. **Зависимости установлены:**
   ```bash
   cd bot
   npm list --depth=0
   ```

3. **Бот запускается:**
   ```bash
   cd bot
   npm start
   # Должно быть: "✅ Бот запущен (polling)" или "✅ Webhook установлен"
   ```

4. **Фронтенд открывается:**
   ```bash
   npm run preview
   # Открой http://localhost:3000 в браузере
   ```

---

## 🔧 Полезные команды

```bash
# Обновить код с GitHub
git pull origin main

# Посмотреть изменения
git status
git diff

# Закоммитить и запушить изменения
git add .
git commit -m "Описание изменений"
git push origin main

# Перезапустить бота (если запущен локально)
# Ctrl+C для остановки, затем npm start

# Посмотреть логи бота на Render
# Зайди на render.com → твой сервис → Logs
```

---

## 📞 Если что-то не работает

1. **Бот не отвечает:**
   - Проверь `BOT_TOKEN` в `bot/.env`
   - Проверь что бот запущен (`npm start` в папке `bot/`)
   - Проверь логи: `tail -f bot.log` (если логи пишутся в файл)

2. **Mini App не открывается:**
   - Проверь `MINI_APP_URL` в `bot/.env`
   - Проверь что GitHub Pages активен (Settings → Pages)

3. **Ошибки базы данных:**
   - Проверь `SUPABASE_URL` и `SUPABASE_SERVICE_KEY`
   - Убедись что таблицы созданы (см. `docs/ДЕПЛОЙ_НА_RENDER.md`)

4. **Проблемы с оплатой:**
   - Проверь `HOT_API_JWT` и `HOT_WEBHOOK_SECRET`
   - Проверь что webhook настроен в панели HOT Pay

---

## 💡 Советы

- **Работай в ветке `main`** — весь проект использует только эту ветку
- **Синхронизируй `public/index.html` и `bot/public/index.html`** — они должны быть идентичны
- **Перед push проверяй изменения:** `git diff`
- **Используй Cursor** — в проекте уже настроены правила в `.cursorrules`

---

## 📚 Дополнительная документация

- `docs/GIT_WORKFLOW.md` — как работать с git в этом проекте
- `docs/ДЕПЛОЙ_НА_RENDER.md` — настройка деплоя на Render
- `docs/PAYMENT_FLOW_ARCHITECTURE.md` — архитектура оплаты
- `PROJECT_BRIEF.md` — описание проекта и целей
