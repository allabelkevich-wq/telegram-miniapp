# Работа с проектом YupSoul с двух компьютеров

Инструкция по переносу проекта на второй компьютер и синхронной работе через Git.

---

## 📋 Что нужно на новом компьютере

### 1. Установить базовые инструменты

```bash
# Node.js (версия 18+)
# Скачать с https://nodejs.org/

# Git
# Скачать с https://git-scm.com/

# Cursor IDE
# Скачать с https://cursor.sh/
```

---

## 🚀 Первоначальная настройка на новом компьютере

### Шаг 1: Клонировать репозиторий

```bash
# Перейди в папку, где хочешь разместить проект
cd ~/Projects  # или любая другая папка

# Клонируй репозиторий
git clone https://github.com/allabelkevich-wq/telegram-miniapp.git

# Перейди в папку проекта
cd telegram-miniapp
```

### Шаг 2: Настроить Git (если ещё не настроен)

```bash
# Укажи своё имя и email (используй те же, что на первом компьютере)
git config --global user.name "Твоё Имя"
git config --global user.email "твой@email.com"

# Проверь настройки
git config --global --list
```

### Шаг 3: Установить зависимости

```bash
# Зависимости для бота
cd bot
npm install
cd ..

# Зависимости для локального просмотра (опционально)
npm install
```

### Шаг 4: Настроить переменные окружения

```bash
# Скопируй файл с примером
cp bot/.env.example bot/.env

# Открой bot/.env в редакторе и заполни:
# - BOT_TOKEN (из @BotFather)
# - ADMIN_TELEGRAM_IDS (твой Telegram ID)
# - SUPABASE_URL и SUPABASE_SERVICE_KEY (из Supabase)
# - MINI_APP_URL (https://telegram-miniapp-ar09.onrender.com/app)
# - и другие переменные по необходимости
```

**ВАЖНО:** Файл `.env` не попадает в Git (он в `.gitignore`), поэтому его нужно настроить на каждом компьютере отдельно.

### Шаг 5: Открыть проект в Cursor

```bash
# Открой Cursor и выбери:
# File → Open Folder → выбери папку telegram-miniapp

# Или из терминала:
cursor .
```

---

## 🔄 Ежедневная работа с двух компьютеров

### Правило №1: Всегда синхронизируйся перед началом работы

**На ЛЮБОМ компьютере перед началом работы:**

```bash
# 1. Получи последние изменения с GitHub
git pull origin main

# 2. Проверь статус (должно быть чисто)
git status
```

### Правило №2: Коммить и пушить после каждой завершённой задачи

**После завершения работы на ЛЮБОМ компьютере:**

```bash
# 1. Проверь что изменилось
git status
git diff

# 2. Добавь изменения
git add public/index.html bot/public/index.html
# или добавь все изменённые файлы:
git add .

# 3. Сделай коммит с понятным описанием
git commit -m "Описание изменений"

# 4. Отправь на GitHub
git push origin main
```

### Правило №3: Если возник конфликт

Если на втором компьютере ты забыл сделать `git pull` и начал работать:

```bash
# 1. Попробуй получить изменения
git pull origin main

# Если Git говорит о конфликте:
# 2. Открой конфликтующий файл в Cursor
# 3. Найди строки с <<<<<<< HEAD и >>>>>>> 
# 4. Выбери нужную версию или объедини вручную
# 5. Удали маркеры конфликта
# 6. Сохрани файл

# 7. Добавь разрешённый конфликт
git add <файл-с-конфликтом>

# 8. Завершить merge
git commit -m "Merge: разрешён конфликт"

# 9. Отправь на GitHub
git push origin main
```

---

## 🎯 Рекомендуемый workflow

### Вариант А: Последовательная работа (проще)

1. **Компьютер 1:** работаешь → commit → push
2. **Компьютер 2:** pull → работаешь → commit → push
3. **Компьютер 1:** pull → работаешь → commit → push
4. И так далее

**Плюсы:** Нет конфликтов  
**Минусы:** Нужно всегда помнить про pull/push

### Вариант Б: Параллельная работа (сложнее)

Если работаешь над **разными файлами** одновременно:

1. **Компьютер 1:** работаешь над `bot/index.js`
2. **Компьютер 2:** работаешь над `public/index.html`
3. Оба делают commit → push (Git сам объединит, т.к. файлы разные)

**Плюсы:** Быстрее  
**Минусы:** Если случайно правишь один файл — будет конфликт

---

## 🛠 Полезные команды Git

```bash
# Посмотреть историю коммитов
git log --oneline -10

# Посмотреть что изменилось в конкретном файле
git diff public/index.html

# Отменить локальные изменения (если что-то пошло не так)
git restore public/index.html

# Посмотреть статус всех файлов
git status

# Посмотреть удалённый репозиторий
git remote -v

# Проверить, есть ли новые изменения на GitHub (без скачивания)
git fetch origin
git status
```

---

## 📱 Локальный просмотр Mini App

### На любом компьютере:

```bash
# Запустить локальный сервер для просмотра public/index.html
npm run preview

# Откроется браузер с адресом http://localhost:3000
```

**ВАЖНО:** Это только для просмотра UI. Для полноценного тестирования с Telegram нужно:
- Открыть бота @YupSoul_bot в Telegram
- Нажать кнопку меню или "Открыть приложение"
- Приложение откроется с продакшен URL (Render)

---

## 🔐 Безопасность

### Что НЕ попадает в Git (в `.gitignore`):

- `bot/.env` — секретные ключи (BOT_TOKEN, SUPABASE_KEY и т.д.)
- `node_modules/` — зависимости (устанавливаются через `npm install`)
- `.cursor/` — настройки Cursor IDE
- `*.log` — логи

**Поэтому:**
- Файл `bot/.env` нужно настроить на каждом компьютере вручную
- Или хранить его в безопасном месте (1Password, Bitwarden) и копировать

---

## ✅ Чек-лист для нового компьютера

- [ ] Установлен Node.js 18+
- [ ] Установлен Git
- [ ] Установлен Cursor IDE
- [ ] Склонирован репозиторий (`git clone ...`)
- [ ] Настроен Git (`git config --global user.name/email`)
- [ ] Установлены зависимости (`cd bot && npm install`)
- [ ] Создан и настроен файл `bot/.env`
- [ ] Проверена работа: `npm run preview`
- [ ] Сделан первый `git pull origin main`

---

## 🎓 Типичные ошибки и решения

### Ошибка: "Please commit your changes or stash them"

**Причина:** Есть несохранённые изменения, а ты пытаешься сделать `git pull`

**Решение:**
```bash
# Вариант 1: Закоммить изменения
git add .
git commit -m "WIP: промежуточные изменения"
git pull origin main

# Вариант 2: Временно спрятать изменения
git stash
git pull origin main
git stash pop  # вернуть изменения обратно
```

### Ошибка: "Your branch is behind 'origin/main'"

**Причина:** На GitHub есть новые коммиты, которых нет у тебя локально

**Решение:**
```bash
git pull origin main
```

### Ошибка: "Failed to push some refs"

**Причина:** Кто-то запушил изменения на GitHub раньше тебя

**Решение:**
```bash
# Получи изменения и объедини
git pull origin main

# Если есть конфликты — разреши их (см. выше)

# Потом снова push
git push origin main
```

---

## 💡 Советы для эффективной работы

1. **Делай `git pull` каждый раз перед началом работы** — это займёт 2 секунды и сэкономит часы на разрешение конфликтов

2. **Коммить часто, маленькими порциями** — лучше 5 коммитов по 1 фиче, чем 1 коммит с 5 фичами

3. **Пиши понятные commit messages:**
   - ✅ "Онбординг: добавлен 3-й экран с кнопкой Начать"
   - ❌ "fix"
   - ❌ "update"

4. **Используй Cursor AI на обоих компьютерах** — контекст проекта сохраняется в `.cursorrules`, поэтому AI будет работать одинаково

5. **Синхронизируй .env вручную** — когда добавляешь новую переменную в `bot/.env` на одном компьютере, не забудь добавить её и на втором

---

## 🔗 Полезные ссылки

- **GitHub репозиторий:** https://github.com/allabelkevich-wq/telegram-miniapp
- **Продакшен (Render):** https://telegram-miniapp-ar09.onrender.com
- **Telegram бот:** @YupSoul_bot
- **GitHub Pages:** https://allabelkevich-wq.github.io/telegram-miniapp/

---

## 📞 Если что-то пошло не так

1. Проверь статус: `git status`
2. Посмотри логи: `git log --oneline -5`
3. Если совсем запутался — спроси Cursor AI: "Помоги разобраться с Git, вот вывод git status: ..."
