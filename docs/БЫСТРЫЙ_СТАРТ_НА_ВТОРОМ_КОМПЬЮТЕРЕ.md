# Быстрый старт на втором компьютере

Пошаговый чек-лист для переноса проекта YupSoul на новый компьютер.

---

## ✅ Чек-лист (выполняй по порядку)

### 1. Установи инструменты

- [ ] Node.js 18+ → https://nodejs.org/
- [ ] Git → https://git-scm.com/
- [ ] Cursor IDE → https://cursor.sh/

### 2. Настрой Git

```bash
git config --global user.name "Твоё Имя"
git config --global user.email "твой@email.com"
```

### 3. Склонируй проект

```bash
cd ~/Projects  # или любая папка
git clone https://github.com/allabelkevich-wq/telegram-miniapp.git
cd telegram-miniapp
```

### 4. Создай файл bot/.env

```bash
touch bot/.env
cursor bot/.env  # или nano bot/.env
```

**Скопируй содержимое с первого компьютера:**

На первом компьютере выполни:
```bash
cat bot/.env
```

Скопируй вывод и вставь в `bot/.env` на втором компьютере.

### 5. Установи зависимости

```bash
cd bot
npm install
cd ..

# Опционально (для локального просмотра):
npm install
```

### 6. Проверь, что всё работает

```bash
# Запусти локальный просмотр
npm run preview

# Должен открыться браузер с http://localhost:3000
```

### 7. Открой проект в Cursor

```bash
cursor .
```

---

## 🔄 Ежедневная работа (важно!)

### Перед началом работы:

```bash
git pull origin main
```

### После завершения работы:

```bash
git add .
git commit -m "Описание изменений"
git push origin main
```

---

## 🎯 Готово!

Теперь можешь работать с проектом на двух компьютерах одновременно.

**Главное правило:** Всегда делай `git pull` перед началом работы и `git push` после завершения!

---

## 📚 Дополнительные инструкции

- **Подробная инструкция:** [`docs/РАБОТА_С_ДВУХ_КОМПЬЮТЕРОВ.md`](РАБОТА_С_ДВУХ_КОМПЬЮТЕРОВ.md)
- **Как передать .env:** [`docs/КАК_ПЕРЕДАТЬ_ENV.md`](КАК_ПЕРЕДАТЬ_ENV.md)
- **Переменные окружения:** [`docs/ENV_ДЛЯ_ВТОРОГО_КОМПЬЮТЕРА.md`](ENV_ДЛЯ_ВТОРОГО_КОМПЬЮТЕРА.md)
