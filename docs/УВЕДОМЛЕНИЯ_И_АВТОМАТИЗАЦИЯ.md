# Уведомления и автоматизация заявок

## 1. Что уже прописано и работает

| Кому | Что происходит |
|------|-----------------|
| **Пользователь** | Сразу после отправки заявки бот пишет в чат: «✅ Заявка принята! Твой персональный звуковой ключ будет создан. Как только он будет готов — пришлю его сюда в чат. Ожидай уведомление.» |
| **Админ (ты)** | В личку с ботом приходит сообщение «🔔 Новая заявка» с именем, языком, датой, местом, запросом и ID заявки. Чтобы это работало: в `.env` указаны `ADMIN_TELEGRAM_IDS`, ты хотя бы раз написала боту `/start`, бот перезапущен. |

То есть: **уведомление админу о новой заявке уже реализовано.** Обещание пользователю «пришлю в чат» есть в тексте, но **само отправление готовой песни пользователю в коде нигде не прописано** — его делают либо вручную, либо автоматическим воркером (см. ниже).

---

## 2. Нужен ли отдельный «сервис», регулирующий заявки?

Сейчас заявки просто лежат в таблице `track_requests` со статусом `pending`. Отдельного «сервиса» в виде отдельного приложения нет: есть только бот (принимает заявки, пишет админу) и фоновая задача, которая считает натальную карту (`computeAndSaveAstroSnapshot`) и сохраняет её в `astro_snapshots`. Кто именно потом генерирует песню и отправляет её пользователю — в коде не заложено.

Варианты:

- **Ручная обработка с телефона:** ты смотришь заявки через `/admin`, где-то вручную (или в другом сервисе) генерируешь песню, потом вручную отправляешь пользователю в Telegram. Регулировать заявки можно, но это трудоёмко и легко что-то забыть.
- **Автоматический воркер:** один процесс (скрипт по крону или постоянный воркер) сам берёт заявки со статусом `pending`, по цепочке: астро → текст (LLM) → аудио (Suno/другой API) → отправка пользователю в боте и смена статуса на `completed`. Тогда отдельного «сервиса» в виде личного труда с телефона не нужно — заявки регулирует этот воркер.

---

## 3. Имеет ли смысл сразу автоматизировать весь процесс?

Да. Сейчас у тебя уже есть:

- Калькулятор натальной карты (астро-снапшот по заявке).
- База данных: заявки, снапшоты, промпты, язык.
- Промпты с подстановкой переменных (`{{astro_snapshot}}`, `{{name}}`, `{{request}}`, `{{language}}` и т.д.).

Чего не хватает для полной автоматизации:

1. **Генерация текста** — один или несколько вызовов LLM (DeepSeek / OpenAI и т.п.) по промпту с данными заявки и астро-снапшота → текст песни и/или описание.
2. **Генерация аудио** — вызов Suno (или другого API) по тексту → получение ссылки/файла на трек.
3. **Отправка пользователю** — бот по `telegram_user_id` из заявки отправляет сообщение с аудио (или ссылкой) и текстом вроде «Твой звуковой ключ готов».
4. **Обновление статуса** — поставить заявке `status = 'completed'` (и при ошибке — `failed` или повтор).

После этого ручная обработка с телефона не обязательна: заявки будут «регулироваться» сами воркером.

---

## 4. Краткий вывод

- **Уведомление о новой заявке** — уже прописано: админу приходит в личку с ботом; пользователю — «заявка принята, пришлю в чат».
- **Уведомление пользователя о готовой песне** — в коде пока нет: его нужно либо делать вручную (ты отправляешь аудио в чат), либо реализовать в автоматическом воркере (воркер в конце пайплайна отправляет сообщение пользователю и ставит `completed`).
- **Сервис, регулирующий заявки** — отдельное приложение не обязательно; достаточно одного воркера (скрипт/cron или долгоживущий процесс), который обрабатывает `pending` заявки по цепочке астро → текст → аудио → отправка в боте.
- **Имеет смысл автоматизировать весь процесс** — тогда с телефона ничего отрабатывать не нужно; достаточно настроить ключи (LLM, Suno) и запустить воркер на сервере.

Подробная схема пайплайна и компонентов описана в `docs/ASTRO_AUTOMATION.md`.
